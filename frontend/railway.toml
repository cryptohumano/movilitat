# Railway – Frontend (monorepo)
# Deploy con Dockerfile: no definir startCommand para usar CMD del Dockerfile (nginx).
# Variable de entorno en Railway: VITE_API_URL = https://TU-BACKEND.up.railway.app/api

[build]
# Si Railway hace build por fuente en vez de Docker, usa esto:
# buildCommand = "yarn install && yarn workspace frontend run build"

[deploy]
# No startCommand: la imagen Docker es nginx y su CMD es "nginx -g daemon off;"
healthcheckPath = "/"
healthcheckTimeout = 30
